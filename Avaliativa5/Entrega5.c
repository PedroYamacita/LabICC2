#include "util.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    int tamanho;
    int vetor[1000000000];
    
    scanf(" %d", &tamanho);
    
    char* formato;
    scanf(" %s", formato);

    if (strncmp(formato, "random", 6)==0){
        for
    }
}